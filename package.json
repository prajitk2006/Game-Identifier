{
  "name": "game-identifier",
  "version": "1.0.0",
  "description": "AI-powered game screenshot identification system",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd my-app && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../my-app && npm install"
  },
  "keywords": ["game", "identifier", "ai", "screenshot", "ml"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

